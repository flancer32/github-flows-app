# github-flows-app
Host application for running @teqfw/github-flows as an event-driven agent execution runtime.

Static logs can be published through `web/` by pointing `WORKSPACE_ROOT` to a directory under the web root, for example `WORKSPACE_ROOT=./var/work`.
